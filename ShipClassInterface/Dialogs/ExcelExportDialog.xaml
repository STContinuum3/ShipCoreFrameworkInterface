<UserControl x:Class="ShipClassInterface.Dialogs.ExcelExportDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Width="400"
             d:DesignHeight="200" d:DesignWidth="400">
    
    <materialDesign:Card Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Icon and Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <materialDesign:PackIcon Kind="MicrosoftExcel" 
                                         Width="32" Height="32" 
                                         Foreground="{DynamicResource PrimaryHueMidBrush}"
                                         VerticalAlignment="Center"
                                         Margin="0,0,12,0"/>
                <TextBlock Text="Export Complete" 
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- Message -->
            <TextBlock Grid.Row="1" 
                       Text="Export completed successfully!"
                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                       Margin="0,0,0,8"/>
            
            <TextBlock Grid.Row="2" 
                       Text="Would you like to open the Excel file?"
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Opacity="0.8"
                       Margin="0,0,0,24"/>
            
            <!-- Buttons -->
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button x:Name="NoButton"
                        Content="NO"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="0,0,8,0"
                        Width="100"
                        Click="NoButton_Click"/>
                <Button x:Name="YesButton"
                        Content="YES, OPEN FILE"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Width="130"
                        Click="YesButton_Click"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>