<UserControl x:Class="ShipClassInterface.Views.BlockGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShipClassInterface.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:componentModel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1400">
    
    <UserControl.Resources>
        <CollectionViewSource x:Key="SortedBlockGroups" Source="{Binding BlockGroups}">
            <CollectionViewSource.SortDescriptions>
                <componentModel:SortDescription PropertyName="Name" Direction="Ascending"/>
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <materialDesign:Card Grid.Row="0" Padding="8" Margin="0,0,0,16">
            <ToolBar Style="{StaticResource MaterialDesignToolBar}">
                <Button Command="{Binding NewConfigurationCommand}"
                        ToolTip="New Configuration">
                    <materialDesign:PackIcon Kind="FileDocumentAdd"/>
                </Button>
                <Separator/>
                <Button Command="{Binding DataContext.LoadAnyXmlFileCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        ToolTip="Load any SE XML configuration file - automatically detects file type">
                    <materialDesign:PackIcon Kind="FileUpload"/>
                </Button>
                <Button Command="{Binding SaveConfigurationCommand}"
                        ToolTip="Save Configuration">
                    <materialDesign:PackIcon Kind="ContentSave"/>
                </Button>
                <Button Command="{Binding SaveAsConfigurationCommand}"
                        ToolTip="Save As...">
                    <materialDesign:PackIcon Kind="ContentSaveAll"/>
                </Button>
                <Separator/>
                <Button Command="{Binding AddBlockGroupCommand}"
                        ToolTip="Add Block Group">
                    <materialDesign:PackIcon Kind="Plus"/>
                </Button>
                <Button Command="{Binding RemoveBlockGroupCommand}"
                        ToolTip="Remove Block Group">
                    <materialDesign:PackIcon Kind="Delete"/>
                </Button>
                <Separator/>
                <TextBlock VerticalAlignment="Center" Margin="8,0">
                    <Run Text="Current File: "/>
                    <Run Text="{Binding CurrentFilePath}" FontWeight="Bold"/>
                </TextBlock>
            </ToolBar>
        </materialDesign:Card>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Block Group List -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <materialDesign:ColorZone Mode="PrimaryDark" Padding="8">
                        <TextBlock Text="Block Groups" FontSize="16" FontWeight="Bold"/>
                    </materialDesign:ColorZone>
                    
                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding Source={StaticResource SortedBlockGroups}}"
                             SelectedItem="{Binding SelectedBlockGroup}"
                             Margin="8">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8" Margin="0,2">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Opacity="0.7" FontSize="12">
                                            <Run Text="{Binding BlockTypes.Count, Mode=OneWay}"/>
                                            <Run Text=" block types"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </materialDesign:Card>
            
            <!-- Block Group Details -->
            <materialDesign:Card Grid.Column="1" Margin="8,0,0,0">
                <Grid>
                    <!-- Selected Block Group Content -->
                    <Grid DataContext="{Binding SelectedBlockGroup}">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Group Name -->
                        <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryLight" Padding="16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0" Text="Group Name:" VerticalAlignment="Center" Margin="0,0,16,0" FontWeight="Bold"/>
                                <TextBox Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                         materialDesign:HintAssist.Hint="Enter group name"
                                         FontSize="16"
                                         ToolTip="A unique name for this block group. This name will be referenced in ship core configurations to apply limits and restrictions."/>
                            </Grid>
                        </materialDesign:ColorZone>
                        
                        <!-- Add Block Type Button -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="16,16,16,8">
                            <Button Command="{Binding DataContext.AddBlockTypeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Add Block Type"/>
                                </StackPanel>
                            </Button>
                            
                            <TextBlock VerticalAlignment="Center" Margin="16,0,0,0" Opacity="0.7">
                                <Run Text="Total Block Types: "/>
                                <Run Text="{Binding BlockTypes.Count, Mode=OneWay}" FontWeight="Bold"/>
                            </TextBlock>
                        </StackPanel>
                        
                        <!-- Block Types List -->
                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="16,0,16,16">
                            <ItemsControl ItemsSource="{Binding BlockTypes}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <materialDesign:Card Margin="0,4" Padding="12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <TextBox Grid.Column="0" 
                                                         Text="{Binding TypeId, UpdateSourceTrigger=PropertyChanged}" 
                                                         materialDesign:HintAssist.Hint="Type ID"
                                                         Margin="0,0,8,0"
                                                         ToolTip="The main type identifier for the block (e.g., 'MyObjectBuilder_Drill', 'MyObjectBuilder_Thrust'). This defines the category of blocks."/>
                                                
                                                <TextBox Grid.Column="1" 
                                                         Text="{Binding SubtypeId, UpdateSourceTrigger=PropertyChanged}" 
                                                         materialDesign:HintAssist.Hint="Subtype ID (optional)"
                                                         Margin="0,0,8,0"
                                                         ToolTip="Optional specific subtype identifier for the block (e.g., 'LargeBlockDrill', 'SmallBlockDrill'). Leave empty to include all subtypes of the Type ID."/>
                                                
                                                <TextBox Grid.Column="2" 
                                                         Text="{Binding CountWeight, UpdateSourceTrigger=PropertyChanged}" 
                                                         materialDesign:HintAssist.Hint="Count Weight"
                                                         Margin="0,0,8,0"
                                                         ToolTip="The weight of this block type when counting towards limits. For example, a heavy block might have weight 2, counting as 2 blocks towards the limit."/>
                                                
                                                <Button Grid.Column="3"
                                                        Command="{Binding DataContext.RemoveBlockTypeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource MaterialDesignFlatButton}"
                                                        Padding="4">
                                                    <materialDesign:PackIcon Kind="Delete" Width="20" Height="20"/>
                                                </Button>
                                            </Grid>
                                        </materialDesign:Card>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                    
                    <!-- Empty State -->
                    <Grid>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedBlockGroup}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="SelectGroup" Width="64" Height="64" 
                                                      HorizontalAlignment="Center" Opacity="0.3"/>
                            <TextBlock Text="Select a block group to edit" 
                                       FontSize="16" Opacity="0.5" Margin="0,16,0,0"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>